{
  "private": true,
  "scripts": {
    "test": "nbb --classpath src:test:../yakuwari/src -e \"(require '[clojure.test :as t] 'yakuwari-view.core-test) (let [r (t/run-tests 'yakuwari-view.core-test)] (.exit js/process (if (pos? (+ (:fail r) (:error r))) 1 0)))\""
  }
}
